import { Text, View } from "react-native";

export default function FunctionalComp() {
    return (
        <View>
            <Text>This is a simple functional component example.</Text>
        </View>
    );
}